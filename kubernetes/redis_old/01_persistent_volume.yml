apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-ebs-claim
  namespace: development
  labels:
    app.kubernetes.io/team: database
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 15Gi